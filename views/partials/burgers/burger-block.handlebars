<li>
	{{burger_name}}
	<br>
	<button type="button" class="devour-burger btn btn-danger" data-id="{{id}}" data-newBurger="{{devour}}">
		{{#if devour}}Devour!{{else}}Regurgitate{{/if}}
		</button>
</li>
